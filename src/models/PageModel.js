export class PageModel {
    constructor(id, title, slug, template, content) {
        this.id = id;
        this.title = title;
        this.slug = slug;
        this.template = template;
        this.content = content;
    }
}